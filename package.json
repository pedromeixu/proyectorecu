{
  "name": "proyectorecu",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run json-server\" \"npm run dev\"",
    "json-server": "cd backend && npx json-server --watch db.json --port 3000",
    "dev": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
